<h1>My Garage</h1>
<h2><%= link_to "Home", cars_path %></h2>
<h2><%= link_to "Garage", favourites_path %></h2>

<ul>
  <% @favourites.each do |favourite| %>
  <li><%= favourite.car.brand %> | <%= favourite.car.model %> | <%= link_to "🗑", favourite_path(favourite), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" } %></li>
  <% end %>
</ul>
